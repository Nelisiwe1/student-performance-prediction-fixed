# Student Performance Prediction (Flask)

This fixed version uses the correct column names from UCI Student Performance dataset.

Quick start:
1. Install dependencies:
   pip install -r requirements.txt
2. Download CSV and save as data/StudentsPerformance.csv
3. Run model training:
   python notebooks/03_model_training.py
4. Start Flask app:
   python app/app.py
